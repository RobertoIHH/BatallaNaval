<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BatallaNavalGame" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorPrimaryVariant">@color/blue_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.BatallaNaval" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/guinda_ipn_primary</item>
        <item name="colorPrimaryVariant">@color/guinda_ipn_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Colores secundarios -->
        <item name="colorSecondary">@color/guinda_ipn_light</item>
        <item name="colorSecondaryVariant">@color/guinda_ipn_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Estado de la barra -->
        <item name="android:statusBarColor">@color/guinda_ipn_dark</item>

        <!-- Otros atributos -->
        <item name="android:windowBackground">@color/gris_claro</item>
    </style>

</resources>